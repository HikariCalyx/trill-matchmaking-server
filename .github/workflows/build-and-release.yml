name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    name: Build ${{ matrix.target }}
    runs-on: ${{ matrix.runs-on }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - runs-on: ubuntu-22.04
            target: x86_64-unknown-linux-gnu
            binary-name: trill-signaling-server_amd64-linux
            asset-path: target/release/trill-signaling-server
            
          - runs-on: ubuntu-22.04-arm
            target: aarch64-unknown-linux-gnu
            binary-name: trill-signaling-server_arm64-linux
            asset-path: target/release/trill-signaling-server
            
          - runs-on: ubuntu-22.04-arm
            target: armv7-unknown-linux-gnueabihf
            binary-name: trill-signaling-server_armv7-linux
            asset-path: target/release/trill-signaling-server
            setup-cmd: sudo apt-get update && sudo apt-get install -y gcc-arm-linux-gnueabihf
            
          - runs-on: ubuntu-22.04-arm
            target: armv6-unknown-linux-gnueabihf
            binary-name: trill-signaling-server_armv6-linux
            asset-path: target/release/trill-signaling-server
            setup-cmd: sudo apt-get update && sudo apt-get install -y gcc-arm-linux-gnueabihf
            
          - runs-on: windows-latest
            target: x86_64-pc-windows-msvc
            binary-name: trill-signaling-server_amd64-windows.exe
            asset-path: target/release/trill-signaling-server.exe

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install cross-compilation dependencies
        if: matrix.setup-cmd != ''
        run: ${{ matrix.setup-cmd }}

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}

      - name: Build release binary
        run: cargo build --release --target ${{ matrix.target }}

      - name: Prepare binary (Unix)
        if: runner.os != 'Windows'
        run: |
          cp ${{ matrix.asset-path }} ${{ matrix.binary-name }}
          xz -9 -e ${{ matrix.binary-name }}

      - name: Prepare binary (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          Copy-Item "${{ matrix.asset-path }}" -Destination "${{ matrix.binary-name }}"
          $ProgressPreference = 'SilentlyContinue'
          # Install xz if not present
          if (-not (Get-Command xz -ErrorAction SilentlyContinue)) {
            choco install xz -y
          }
          xz -9 -e "${{ matrix.binary-name }}"

      - name: Upload release artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.binary-name }}.xz
          path: ${{ matrix.binary-name }}.xz
          retention-days: 1

  release:
    name: Create Release
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: artifacts/**/*.xz
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
